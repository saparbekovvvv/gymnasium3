import scss from "./GalleryContent.module.scss";
const GalleryContent = () => {
  return (
    <section className={scss.content}>
      <div className="container">GalleryContent</div>
    </section>
  );
};

export default GalleryContent;
