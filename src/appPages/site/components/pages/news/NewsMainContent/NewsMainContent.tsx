import scss from "./NewsMainContent.module.scss";
const NewsMainContent = () => {
  return (
    <section className={scss.content}>
      <div className="container">NewsMainContent</div>
    </section>
  );
};

export default NewsMainContent;
